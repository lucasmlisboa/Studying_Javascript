// Operadores lógicos ou ternário 
// tem um cliente e se esse cliente tiver + 100 pontos é premium, contrário disso comum
let ponto =100;
let tipo = ponto > 100 ? 'premium' : 'comum'; // if
console.log(tipo);

